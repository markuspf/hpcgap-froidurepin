The HPC-GAP package `froidurepin'
=================================


This is an experimental package that implements sequential and
parallelised variants of Froidure's and Pin'd algorithm for semigroup
enumeration.
